.userinfo {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

.userinfo__title {
    font-size: 18px;
    color: rgba($text-color, .4);
    font-weight: 600;
    margin-bottom: 20px;
    /* line-height: 1; */
    text-align: center;
}

.userinfo__name {
    font-weight: 800;
     font-size: 72px;
     line-height: 1;
}